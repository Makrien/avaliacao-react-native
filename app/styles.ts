import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#066cff",
    // background: "linear-gradient(90deg, rgba(6,108,255,1) 0%, rgba(0,212,255,1) 100%)",
  },
  gradient: {
    width: '100%',
    height: '100%'
  }
});

export default styles